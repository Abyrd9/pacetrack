### MISCELLANEOUS SELF NEEDED KEYS ###
# Add origins for CORS
CORS_ORIGIN_URLS=http://localhost:3000
# Used for signing cookies
SESSION_SECRET=wzvo1Wi5unO3FYD5Lo9GALDSuiNImFHS
# Used on Railway to setup the API to listen on the internal networking
INTERNAL_PORT= # Keep this empty when running locally
# This is how we change the default PORT on railway
PORT= # don't need this locally

### DATABASES ###
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/pacetrack
REDIS_URL=redis://localhost:6379

### EMAIL ###
# We use mailpit for local dev with the EMAIL_HOST and EMAIL_PORT
# But we'll use resend in production
RESEND_API_KEY=
EMAIL_HOST=127.0.0.1 # Dev Only
EMAIL_PORT=1025 # Dev Only

### STRIP ###
# Removing Secret Key allows app to function without stripe
# We run mock stripe locally through so this key can be bunk
STRIPE_SECRET_KEY=sk_test_123
STRIPE_API_BASE=http://localhost:12111 # Dev Only
STRIPE_WEBHOOK_SECRET=whsec_mocked
STRIPE_PRICE_ID=price_mock_123

### S3 (MinIO) ###
# Defaults are set for DEV (This needs to be set for PROD though)
# S3_ENDPOINT=https://c073d423106e50501fe811f9d6bc7644.r2.cloudflarestorage.com
# S3_ACCESS_KEY=e3eeb7e7dc0c88ea0c9caa254e9afac5
# S3_SECRET_KEY=61d0ac904172d1d342fc318c2b55a34d57bbf8b3a7900a52006568d32b6a4348

### LOGGING ###
LOG_LEVEL=debug